server:
  port: 8080
  address: localhost

spring:
  application:
    name: sexhub-server
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 30MB
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      lettuce:
        pool:
          max-active: 8
          max-wait: -1
          max-idle: 8
          min-idle: 1